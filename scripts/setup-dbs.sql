-- ==========================================
-- SYS INESCOLARA — Database Setup Script
-- ==========================================
-- Run this script against your MySQL server
-- to create both required databases.
-- ==========================================

CREATE DATABASE IF NOT EXISTS `sysinescolara`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

CREATE DATABASE IF NOT EXISTS `SysInescolara-Seguridad`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
